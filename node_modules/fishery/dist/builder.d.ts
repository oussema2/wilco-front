import { GeneratorFn, HookFn, DeepPartial, CreateFn } from './types';
export declare class FactoryBuilder<T, I> {
    private generator;
    private sequence;
    private params;
    private transientParams;
    private associations;
    private afterBuilds;
    private onCreates;
    constructor(generator: GeneratorFn<T, I>, sequence: number, params: DeepPartial<T>, transientParams: Partial<I>, associations: Partial<T>, afterBuilds: HookFn<T>[], onCreates: CreateFn<T>[]);
    build(): T;
    create(): Promise<T>;
    setAfterBuild: (hook: HookFn<T>) => void;
    setOnCreate: (hook: CreateFn<T>) => void;
    _mergeParamsOntoObject(object: T): void;
    _callAfterBuilds(object: T): void;
    _callOnCreates(object: T): Promise<T>;
}
