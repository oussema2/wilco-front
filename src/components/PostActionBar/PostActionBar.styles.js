import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create( {
	actionBar: {
		flexDirection: 'row',
		justifyContent: 'space-evenly',
		marginTop: 24,
		paddingBottom: 20
	}
} );
