import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create( {
	title: {
		marginBottom: 16
	},
	emptyState: {
		marginBottom: 20
	},
	containerView: {
		marginBottom: 20
	}
} );
