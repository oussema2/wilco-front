import { StyleSheet } from 'react-native';
import { palette } from '../../Theme';

export const styles = StyleSheet.create( {
	lineStyle: {
		borderTopWidth: 1,
		borderColor:
		palette.grayscale.mercury,
		marginBottom: 32
	}
} );
